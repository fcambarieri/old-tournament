/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package com.thorplatform.swing.actions;

/**
 *
 * @author fernando
 */
public interface SwingActionListener {
    /*
     * Paso como parametro un boolean avisando si se hizo un login.
     * @param boolean true el usuario se logueo correctamente, se lo contrario
     * se deberia pasar false.
     */
    void setIsLogin(boolean isLogin);
}
